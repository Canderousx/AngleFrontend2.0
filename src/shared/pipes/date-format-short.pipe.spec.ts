import { DateFormatShortPipe } from './date-format-short.pipe';

describe('DateFormatShortPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatShortPipe();
    expect(pipe).toBeTruthy();
  });
});
